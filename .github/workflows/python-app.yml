# This workflow will install Python dependencies, run tests and ruff with a matrix of Python versions

name: Basic tests

on:
  push:
    branches: [ "main" ]
  # Every pull request, not only those targeting main: a stacked pull request
  # (based on another feature branch) otherwise gets no checks at all, and on
  # this repository merging to main auto-publishes a release.
  pull_request:

permissions:
  contents: read

jobs:
  test:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov
        pip install .
    - name: Lint with Ruff
      run: |
        pip install ruff==0.16.0
        ruff check --output-format=github .
      continue-on-error: false
    - name: Test with pytest and coverage
      run: |
        pytest --cov=tools4vasp --cov-report=term-missing --cov-report=xml
    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      if: matrix.python-version == '3.12'
      with:
        name: coverage-report
        path: coverage.xml
    - name: Run ShellCheck
      uses: azohra/shell-linter@latest
      with:
        path: "src/tools4vasp/bash_scripts"
